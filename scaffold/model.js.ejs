'use strict';

/*!
 * <%=: name | capitalize %> schema and model
 */

var mongoose = require('mongoose');


/**
 * <%=: name | capitalize %> Schema
 */

var <%=: name | capitalize %>Schema = new mongoose.Schema({
  name: String,
  created: { type: Date, default: Date.now },
  updated: { type: Date, default: Date.now }
});


/**
 * Pre-save hook
 */

<%=: name | capitalize %>Schema.pre('save', function(next) {
  this.updated = new Date;
  next();
});


/**
 * Create the mongoose model
 */

mongoose.model('<%=: name | capitalize %>', <%=: name | capitalize %>Schema);
