# <%=: name | capitalize %> model

mongoose = require 'mongoose'

###
<%=: name | capitalize %> Schema
###

<%=: name | capitalize %>Schema = new mongoose.Schema(
  name: String
  created: { type: Date, default: Date.now }
  updated: { type: Date, default: Date.now }
)

###
Pre-save hook
###

<%=: name | capitalize %>Schema.pre 'save', (next) ->
  @updated = new Date()
  next()

mongoose.model '<%=: name | capitalize %>', <%=: name | capitalize %>Schema
